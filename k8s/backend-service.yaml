apiVersion: v1
kind: Service
metadata:
  name: llm-backend-service
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
